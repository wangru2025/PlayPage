# 汪汪奶茶店

这是一个纯静态互动游戏示例，入口文件是 `index.html`。

- 使用作品互动 API 云存档。
- 数据表：`milk_tea_saves`。
- 用户名和密码只用于生成 `saveKey`，密码不会明文上传。
- 所有玩家提示为中文。
- 成功、失败、进度提示使用 `aria-live` 活动区域。
- 顶部功能切换使用按钮，不使用会导致误解的自定义选项卡语义。
- 文件使用 UTF-8 with BOM 写入，方便 Windows 编辑器识别中文。

注意：这是宣传演示性质的轻量云存档，不要保存敏感信息。
